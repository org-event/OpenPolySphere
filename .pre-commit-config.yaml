# Install: just install  (runs pre-commit install)
# Manual: pre-commit run --all-files

repos:
  - repo: local
    hooks:
      - id: just-check
        name: just check
        entry: just check
        language: system
        pass_filenames: false
        always_run: true
